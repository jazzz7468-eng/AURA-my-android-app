package com.aura.app.ui.theme

import androidx.compose.ui.graphics.Color

// ═══════════════════════════════════════════════════
// KIDS THEME (6-12) — Bright, playful, inviting
// ═══════════════════════════════════════════════════
object KidsColors {
    val Background = Color(0xFFF0E6FF)      // Soft lavender
    val Surface = Color(0xFFFFFFFF)          // Pure white cards
    val SurfaceVariant = Color(0xFFF5F0FF)   // Light purple tint
    val Primary = Color(0xFFFF7043)          // Warm orange
    val Secondary = Color(0xFF66BB6A)        // Fresh green
    val Tertiary = Color(0xFF42A5F5)         // Sky blue
    val OnBackground = Color(0xFF2D1B4E)     // Dark purple text
    val OnSurface = Color(0xFF2D1B4E)
    val OnPrimary = Color(0xFFFFFFFF)
    val Error = Color(0xFFE53935)
    val XPGradientStart = Color(0xFFFF7043)
    val XPGradientEnd = Color(0xFFFFCA28)
    val CardGlow = Color(0x33FF7043)
    val AccentGlow = Color(0x4466BB6A)
}

// ═══════════════════════════════════════════════════
// TEENS THEME (13-21) — Dark, modern, neon-accented
// ═══════════════════════════════════════════════════
object TeensColors {
    val Background = Color(0xFF0F1923)       // Dark charcoal
    val Surface = Color(0xFF1A2634)          // Slightly lighter card
    val SurfaceVariant = Color(0xFF243447)   // Elevated surface
    val Primary = Color(0xFF7C4DFF)          // Electric purple
    val Secondary = Color(0xFFFF4081)        // Hot pink
    val Tertiary = Color(0xFF00E5FF)         // Neon cyan
    val OnBackground = Color(0xFFE8EAF0)     // Light text
    val OnSurface = Color(0xFFE8EAF0)
    val OnPrimary = Color(0xFFFFFFFF)
    val Error = Color(0xFFFF5252)
    val XPGradientStart = Color(0xFF7C4DFF)
    val XPGradientEnd = Color(0xFFFF4081)
    val CardGlow = Color(0x337C4DFF)
    val AccentGlow = Color(0x3300E5FF)
}

// ═══════════════════════════════════════════════════
// ADULTS THEME (22+) — Premium, professional, elegant
// ═══════════════════════════════════════════════════
object AdultsColors {
    val Background = Color(0xFF0B1629)       // Deep navy
    val Surface = Color(0xFF132240)          // Dark card
    val SurfaceVariant = Color(0xFF1B2D52)   // Elevated surface
    val Primary = Color(0xFF00E5FF)          // Cyan
    val Secondary = Color(0xFF7C4DFF)        // Purple
    val Tertiary = Color(0xFFFF6B9D)         // Soft magenta
    val OnBackground = Color(0xFFE0E6F0)     // Light text
    val OnSurface = Color(0xFFE0E6F0)
    val OnPrimary = Color(0xFF0B1629)
    val Error = Color(0xFFFF5252)
    val XPGradientStart = Color(0xFF00E5FF)
    val XPGradientEnd = Color(0xFF7C4DFF)
    val CardGlow = Color(0x3300E5FF)
    val AccentGlow = Color(0x337C4DFF)
}

// ═══════════════════════════════════════════════════
// SHARED COLORS
// ═══════════════════════════════════════════════════
object AuraColors {
    val Gold = Color(0xFFFFD700)
    val XPGreen = Color(0xFF4CAF50)
    val StreakOrange = Color(0xFFFF9800)
    val LevelBlue = Color(0xFF2196F3)
    val SkillEmpathy = Color(0xFFE91E63)
    val SkillConfidence = Color(0xFFFF9800)
    val SkillCommunication = Color(0xFF2196F3)
    val SkillLeadership = Color(0xFF9C27B0)
    val SkillResilience = Color(0xFF4CAF50)
}
